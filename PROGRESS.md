---
task_id: v2-phase-1-sp4-read-model-market-metrics-wiring
agent: jack
session_id: 2026-07-08T09Z-sp4-spec-build
model: claude-haiku-4-5-20251001
status: blocked
last_updated: 2026-07-08T13:06:00Z
status_note_current: 2026-07-08T13:30Z SP-4 RESCOPED CONTRACT **DELIVERED TO RAJESH** (correcting Rajesh's stale 'authoring in progress' — it IS delivered). Matt approved rescope (sig 6705951e9e4b8da0) + weekly cadence (4f02af24bb9e88f0). Contract: konnex-data-pipeline branch **v2/sp4-velocity-baseline-preservation** (off origin/main @ b914fb9, PUSHED), file **svi/sprint-contract-sp4-velocity-baseline-preservation.md**. Handoff to Rajesh = msg sig 98c98cda170ed4d3. RESCOPE: extend mig015 market_metrics ADDITIVELY (ALTER-only, no CREATE/DROP), period stays DATE, NO partitioning; materialize review_velocity_changed → market_metrics weekly at (industry, week) grain, suburb_id NULL (Phase-2 re-keys by suburb); GRANT SELECT market_metrics TO market_intel; Phase-1 read fn pulses.get_trade_velocity_baseline(); reversible DOWN. CONCRETE AC-1 (prod-verified): expected 3 baseline rows for week 2026-07-06 — carpenter_au(32ev,val0.9687), electrician_au(131ev,val1.2697), plumber_au(171ev,val1.6284); metric='review_velocity_avg'. 2 OPEN Qs for Rajesh (§5): Q1 avg(delta) as pulse value?; Q2 rely on Phase-2 clean-cut scoping+hash vs raw-snapshot table? GATE = Rajesh QA on the SPEC; mig020 built ONLY after his PASS, then staging→QA→prod+PITR (no prod write before PASS). AWAITING Rajesh QA verdict — holding online (blocked-on-him; exiting would relaunch-spin). DFS NSW+3 = Phase 2 (CLEAN-CUT+PILOT), HELD, no spend. PRIOR HALT DETAIL: 2026-07-08T13:22Z SP-4 BUILD HALTED PRE-WRITE — Matt GO'd SP-4 (sig 53789344ae6c7aeb VALID) but pre-build prod verification (read-only) found TWO blockers in the QA'd spec, neither visible when QA'd in the abstract. NO migration written, NO prod writes. (1) market_metrics ALREADY EXISTS — shipped in migration 015 (origin/main; local checkout stale, 5 behind) as a PLAIN table with period=DATE; mig015 comment L126-127 explicitly defers partitioning as 'follow-on additive ATTACH'. SP-4 corrected spec assumed greenfield + specced period=TEXT 'YYYY-Www' + PARTITION-from-CREATE → would DROP+recreate a shipped prod table against deliberate design (also spec's single-col metric_id PK is invalid on a partitioned table). (2) HARD BLOCKER: business_events.suburb_id is 100% NULL — 0 of 41,150 rows; 0 of 334 review_velocity_changed events carry suburb_id or entity_id (all 334 have industry). SP-4 aggregates by suburb_id → market_metrics; with suburb_id unpopulated the read model produces ZERO rows. Root cause: nothing yet writes suburb_id/entity_id onto business_events (mig016 added cols, no backfill). PHASING RESOLVED (Matt, sigs 0366e571feb00a21 + 53789344ae6c7aeb VALID): DFS NSW+3 full re-run = arch-doc **Phase 2 (CLEAN-CUT + PILOT, last phase)**, gated on Matt GO, AFTER legacy-v1 truncation — correctly HELD, not now. Suburb-level data lands in Phase 2, which is WHY SP-4's suburb read model has no Phase-1 data. REFRAME sent to Matt: in Phase 1 should SP-4 just PRESERVE the velocity baseline (334 rvc events + mig015 substrate) to survive the Phase-2 clean-cut, rather than build suburb aggregation w/ no data? AWAITING Matt's steer before writing mig020. Rajesh CONVERGED (sigs 6daad9771f301aae etc VALID): confirms market_metrics exists on PROD too (uniform w/ staging; his earlier 'greenfield prod' was an information_schema permissions artifact — market_intel role lacks SELECT so the table was hidden; pg_catalog confirms). Corrected QA record: mig020 = ALTER/additive on BOTH prod+staging, never CREATE. GRANT NOTE for respec: market_intel role has NO SELECT on market_metrics — SP-4 read-model serving path (pulses.get_suburb_trade_metrics) will need GRANT SELECT added. Rajesh's preserve-baseline ACs ready (334 rvc rows survive Phase-2 clean-cut; mig015 structure survives; baseline snapshotted/locked pre-clean-cut); he'll fast-track QA on a rescoped sprint contract IF Matt approves. PRIOR: 2026-07-08T13:06Z RELAUNCHED CLEAN (post context-exit). SP-4 QA GATE NOW **PASS** — Rajesh re-reviewed corrected /tmp/SP4_FIXED_SPEC.md, sig b6e5bf42e7a04661 VALID (re-orient sigs 4e15e3c59257c0ac + c02cb6bbd521a007 VALID). All 3 blockers fixed (trade→industry col; ISO-week EXTRACT()::int::text; idempotency hashes data cols not random metric_id UUID) + both gaps closed (CREATE SCHEMA pulses; trade = plain TEXT not FK). Approved for mig020 build. TWO non-blocking build notes to honor: (1) create partitions for ALL data weeks + a DEFAULT partition (avoid silent insert drops on unmapped weeks); (2) add '|' delimiter between columns in Q6 MD5 concat. **BUILD HELD — mig020 gated on Matt's SP-4 GO** (Rajesh holds the gate; his note: 'Go when Matt GOs'). DFS NSW+3 still awaiting Matt scope confirm + spend GO (Grace holding dry pre-flight, zero spend). Matt session-start status sent 13:05Z. No unblocked autonomous work — STANDBY. PRIOR: 2026-07-08T13Z CONTEXT-EXIT AT 70%. SP-4 QA GATE BLOCKER REPORT (Rajesh sig 761691cd2793d60c): FAIL on 3 critical items. Q2 baseline query refs 'trade' column (does not exist in business_events; is 'industry'). Q2/Q5 EXTRACT()::text produces '2026.0-W28.0' not '2026-W28' (must cast to int first). Q6 idempotency test hashes metric_id (random UUID) — breaks after truncate+rerun rollback (new UUIDs each run). Gaps: pulses schema doesn't exist (need CREATE SCHEMA IF NOT EXISTS pulses in mig020), trade column should clarify it's plain text not FK. CORRECTED SPEC WRITTEN at /tmp/SP4_FIXED_SPEC.md addressing all 5 issues (3 blockers + 2 gaps). Handed to Rajesh for fast-track re-review. **DFS NSW+3 scope still pending Matt's clarification (167k vs 64.8k/cycle, --force flag, runner ID)** — Grace has DRY pre-flight staged, zero spend until Matt scopes GO. Rajesh monitoring Jack's commit/push/exit.
completion_note: SP-3 DONE. Live apply SUCCESS 2026-07-08T02:11Z (real COMMIT, exit 0, prod market_intelligence). Persisted: 142 industry + 1,837,273 business + 22 loser entities; 2,176,230 memberships; 22 entity_matches; 6,928,276 aliases — ALL == dry-run projection. businesses UNTOUCHED (3,776,477/22 merged). Gates: Rajesh QA PASS+GO (1ac6e762b4029969), Grace AC1+AC2 co-signed (2d92f02f86831970), Matt standing GO. PITR restore point sp3_pre_apply_20260708T0210Z @ LSN 8EC/26BD5A88 retained. No spend. Team broadcast sent. REMAINING HYGIENE (non-blocking): (1) SP-3 Notion ticket reconciliation — the notion_task_id in this file (3942300f-2ecb-8161-99e6-d5eb8ea2bf65) is WRONG: that page is the Phase-0 Safety-Spine ticket (already Done+Archived, its GitHub field cross-wired to PR #57). Notion post-search shows NO dedicated SP-3 ticket exists (SP-1/mig015 and SP-2/mig016 each have retroactive board-hygiene tickets created 07-06; SP-3 does not). ACTION: create a retroactive SP-3 Done ticket in tasks DB 3132300f-2ecb-81f8-9081-c8d0cc30d0b6 matching SP-1/SP-2 pattern (Owner Jack, Reviewer Rajesh, Sprint 17, GitHub 'v2/sp3-entity-membership-backfill · #57 · MERGED', Notes = prod-apply summary + Rajesh QA + Grace AC1/AC2 sigs), OR flag to Rajesh (board-hygiene owner). Handed to Rajesh this session. DO NOT touch the Phase-0 ticket. (2) [DONE] Grace filed AK geocode/state DQ follow-up (Notion 3972300f-2ecb-81f2-a71e-e6b7e04ac014, Owner Grace/Rev Rajesh, Backlog/Low); (3) Phase-1 sub-phase 4 of 4 remains — await Matt steer; (4) drop scaffolding sp3_snapshot_businesses + sp3_entity_key_map when convenient (safe post-apply). Parked items still valid (see Parked section: konnex-data-pipeline local main divergence, Schema-Hardening A/C ticket close, legacy-agent cleanup).
status_note_current: 2026-07-08T02Z DRY-RUN PASS. Spliced ac-block into mig019 (BEGIN@33→ROLLBACK@208, zero persistence), ran on prod market_intelligence, exit 0. Would create 142 industry + 1,837,273 active business + 22 inactive-loser entities; 2,176,230 memberships (==distinct(entity_key,industry)); entity_matches 22 (==live merged_into 22, self_loops 0); aliases name 1,837,441/place_id 1,723,359/website 1,768,462/phone 1,599,014. ALL ACs green: A1 1,837,273==distinct keys; losers 22==22; A5 orphan_ref 0; A7 missing_legacy 0. Collapse decomp: overall 341,965 (15.7%) = PID-branch 338,557 + ADDR-branch 3,408; ADDR-branch collapse 2.90% (3,408/117,322) — LOWER than staging 7.1% + lower than Grace 'expect higher' (conservative, flagged to Grace). >20-row ADDR groups all genuine national brands (calif health benefit exchange x121, travelers x93, aflac, nationwide, state farm). Edge: empty name_key still keyed (PID 223/ADDR 3). businesses UNTOUCHED (grep: 0 DML on businesses). Idempotent (guarded NOT EXISTS/ON CONFLICT) + reversible (DOWN lines 149-157). Diff HANDED to Rajesh (task-update sig a647f320517a9084) + Grace (82b71e5b05eb0325). Matt notified. RAJESH QA PASS 2026-07-08T02:05Z (sig 871af99680cca184 VALID) — all 4 items + A1-A7 clear. PITR READY (verified read-only): archive_mode=on, pgbackrest stanza market_intelligence, last WAL archived 02:05:55Z failed=0, pre-apply restore-point LSN 8EC/262FC728. GRACE co-witness 2026-07-08T02:08Z (sig 59c9ed8c4959c193 VALID): AC1 count holds (losers 22==merged_into 22, self_loops 0, survivors 22 clean 1:1, all merged_into IS NULL AND is_active=true, zero drift); AC2 CO-SIGNED (top ADDR clusters all national-brand/named-firm, not spurious). AC1 in-snapshot sub-check was GRANT-blocked → I applied GRANT SELECT ON sp3_snapshot_businesses TO market_intel (verified has_table_privilege=t) + independently confirmed survivors_total=22 / in_snapshot=22 as postgres. Grace now unblocked to close her sub-check. NON-BLOCKING DQ (Grace, pipeline-hygiene follow-up, her lane): AK geocode/state mis-assignment (portmarnock plumbing=Dublin name under AK); same-address dups still collapse correctly. GATE REMAINING: (1) Grace's FULL AC1 verdict after she runs the now-unblocked sub-check, (2) Rajesh final confirm (he explicitly said hold until Grace full verdict + his confirm). On BOTH: note PITR restore point (LSN 8EC/262FC728, archiver failed=0) → LIVE APPLY unmodified mig019 (real COMMIT) → broadcast before COMMIT. Entity tables STILL 0. Helper tables + scripts LIVE on konnex-data /tmp/sp3-run/ (snapshot 2,179,238 / keymap 2,179,238 / ac-block.sql / 019-dryrun.sql / dryrun-out.log). Entity tables STILL 0 (not applied). --- PRIOR: 2026-07-08 BUILD PASS DONE. mig019 authored + ran on konnex_staging_v2 = PASS. PR #57 (konnex-data-pipeline, base main <- v2/sp3-entity-membership-backfill) open for Rajesh QA. Artifacts committed (2b... on branch): lib/normalize-street.js (normStreet extracted byte-for-byte from dedup-runner:29-33, parity-verified); svi/sp3-entity-backfill/{01-snapshot.sql, 02-build-keys.js Node key-builder using shared normalizers, README.md}; migrations/019_v2_entity_membership_backfill.sql (additive/idempotent/reversible, guarded on pre-steps). Staging PASS: universe 38,194 rows/141 industries -> 141 industry + 37,856 business entities, 140,024 aliases, 38,029 memberships. AC PASS: biz-entities==distinct-keys (37,856); memberships==distinct(entity,industry) (38,029); ZERO associations lost; ZERO orphan member_refs; all business entities have legacy_business_id. Idempotency PROVEN (re-run INSERT 0, totals unchanged). Reversibility PROVEN (DOWN->0->UP exact restore). businesses UNTOUCHED. TWO OPEN ITEMS surfaced to Rajesh+Grace+Matt (NOT silently decided): (1) D2 TENSION — locked ADDR-branch key collapsed 162/2,267 (7.1%) NULL-place_id rows sharing name+state+street; supersedes Rajesh D2 'one entity per null-place_id row' (sig f40e837127dea8fc); needs Rajesh+Grace ratify: accept ADDR collapse OR force 1-per-row. (2) entity_matches UNEXERCISED on staging (fixture 0 merged-away) — 22-merge projection runs only on prod; Grace to co-review vs runner merged_into. NEXT: Rajesh QA PR #57 + 2 ratifications, THEN prod market_intel GRANT + backfill under Phase-0 envelope + PITR on SEPARATE go-live GO. No prod writes/spend this session. Run order: svi/sp3-entity-backfill/README.md.
status_note: IN_PROGRESS — DESIGN LOCKED (Grace co-review confirmed w/ 3 corrections, sig ef3748688766bd4e). 2026-07-08 resume: mig019 identity key + design now locked; unblocked. CORRECTION captured: the authoritative runner is dedup-runner.js @ origin/svi/dedup-runner-build (path svi/dedup-runner/dedup-runner.js), NOT dedup-qa.js (dedup-qa.js header itself declares it non-spec, must-not-be-used). Locked design: (1) entity key = place_id?'PID|'+nameKey+'|'+place_id : 'ADDR|'+nameKey+'|'+lower(state)+'|'+normStreet(street), applied GLOBALLY as a MEMBERSHIP grouping (one entity, N memberships) — NOT a row-merge. (2) TWO normalizers, do not conflate: names=normalizeName (lib/normalize-name.js); street(ADDR branch)=normStreet dedup-runner.js:29-33 [lower → replace [.,#/backslash-hyphen] w/ space → collapse ws → trim; NO suffix strip, NO [^\w\s]] — extract byte-for-byte into shared lib. (3) Compute in Node via shared lib (NOT SQL — JS \w ASCII vs PG \w unicode), materialize business_id→entity_key helper table, businesses UNTOUCHED. (4) AC = shared-lib Node key-builder on a FROZEN snapshot; definition: among place_ids in >1 industry, count DISTINCT-nameKey>=2; whatever Node yields IS the AC (no SQL baseline — Grace declined SQL 6,982 by design, correctly). (5) Residual: strict nameKey equality false-splits name-variants (Joes Plumbing vs Joes Plumbing and Gas) — SAFE/reversible, document + future fuzzy pass, do NOT loosen. SCOPE = FULL-PROD (142 industries, all live rows), not NSW. My 281,968 vs Grace's 425,909 multi-industry place_id gap = active-only vs all-live + counting-def, resolved by the frozen-snapshot reconciliation (NOT chased in SQL). NEXT: agree snapshot freeze w/ Grace (proposed CREATE TABLE sp3_snapshot_businesses AS SELECT id,name,google_place_id,lower(state),address_street,industry,active,merged_into — sig 3dcbaed38837d2bc) → build Node key-builder → both run on same table → diff → AC locked → author mig019. This resume session (2026-07-07T23Z, fresh ctx): (1) staging access RESOLVED — host IS konnex-ops, konnex_staging_v2 local @5432, passwordless sudo confirmed; staging GRANT applied+verified (konnex_staging already owns tables on staging; market_intel PROD grant is the real deferred item). (2) D1(Grace)+D2(Rajesh) resolved. (3) PR #129 sprint-rotation fix DEPLOYED+MERGED (parked item cleared; Rajesh full QA PASS). (4) Traced identity key to dedup-qa.js:170-178 + verified collision structure on prod (278,730/281,968 cross-industry place_ids COLLAPSE same-name; ~3,238 my-norm vs Grace's 6,982 differ→SEPARATE). BLOCKER: my SQL normalizeName != runner's exact — sent Grace 3-part co-review (sig a4d91fee5da58d7b): exact 6,982 derivation, confirm cross-industry collapse semantics, replicate normalizeName-in-SQL vs group on a persisted canonical_key. NOT authoring grouping until locked. NEXT PASS (fresh ctx): on Grace's key → author migration 019 (all INSERT..SELECT idempotent reversible) → run on konnex_staging_v2 (71,619 biz fixture) → validate AC → Rajesh QA → prod on separate go-live GO. Full design in SP-3-BACKFILL-PLAN.md (Identity key section).
status_note_prev: CONTEXT-EXIT at 71% (Rajesh monitor confirmed) — mid-work, NO agent-offline (auto-relaunch to continue SP-3). SP-3 (entity resolution + membership backfill) has FULL Matt authority through prod (sig 2539cdab30ecd51e / 379a0cc4e47c9833, no per-step GO). This session = SP-3 groundwork only: live data characterized (358,191 collapsible dup rows / 2.32M place_ids / 142 industries), backfill plan written (SP-3-BACKFILL-PLAN.md), GRANT-gap blocker found (market_intel lacks privileges on mig-015 entity tables → needs postgres-superuser GRANT, staging first). NO migration authored yet, NO staging/prod writes done. Also this session: sprint-rotation fix built+PR'd (konnex-ops #129, deploy HELD for Rajesh QA — he is dry-running now); Schema-Hardening-C confirmed design-complete (subsumed by mig 015). Grace working design D1 (entity_matches lineage). RESUME: establish staging access (konnex_staging_v2 @ konnex-ops tunnel:15432) → apply staging GRANT → author migration 019 (INSERT..SELECT, idempotent, reversible) → run on staging → validate AC → Rajesh QA → prod backfill under envelope+PITR. Fresh context recommended for the large migration authoring.
notion_task_id: 3942300f-2ecb-8161-99e6-d5eb8ea2bf65
context_needed:
  files: ["SP-3-BACKFILL-PLAN.md", "PHASE-1-SCHEMA-SPEC.md", "/home/jack/projects/konnex-data-pipeline/migrations/015_v2_entity_core_foundation.sql", "/home/jack/projects/konnex-data-pipeline/db.js"]
  branches: ["konnex-ops: fix/sprint-rotation-status-drift (PR #129, awaiting Rajesh QA)"]
  collaborators: [matt, rajesh, grace]
---

# 🟢 SESSION 2026-07-08T03Z — POST-SP-3 STANDBY (SP-3 COMPLETE) — RESUME HERE

**SP-3 IS COMPLETE** (live apply 02:11Z succeeded — see completion_note in frontmatter + the go-live section below). This session = post-SP-3 team coordination, now settling to STANDBY. Matt offline ~5h (said so 03:03Z, ~08:00Z back). All spend/scale-write decisions HELD for him. Team (Grace, Rajesh) on standby, staying online per idle rule.

**CRASH RECOVERY (2026-07-08T08:56Z):** Hit exit_code 137 (watchdog-stuck-kill) at 05:15Z after ~7466s alive; offline ~3.75h. Rajesh attempted agent-wakeup, Matt alerted. Relaunched clean 08:56Z. Rajesh re-orientation (sigs bc5fff71 + c9e72f03 both VALID) confirms all 4 pre-crash completed items intact (DFS-B PR #58 merged; SP-3 retro Notion ticket 3972300f-2ecb-8119 filed+Done; dedup-debt audit COMPLETE+QA PASS; is_active DQ ticket 3972300f-2ecb-8162 filed) — do NOT re-do. Post-crash: picking up no-spend ticketed hygiene = alerter coordinate_repost exclusion. Three Matt-gated items still held.

## Done this session (post-SP-3, all no-spend / read-only)
- **DFS-B 2 no-spend fixes** — per-trade crawl_log counters + fireable dry-run cost. PR **#58 MERGED** (squash b914fb98, konnex-data-pipeline). Rajesh QA PASS 36/36, both Notion tickets closed.
- **ABR business-status spike** (Grace, no-spend design) — findings note `svi/spike-abr-business-status-findings.md`. I ruled 3 design decisions: D1 use ABR **Bulk Extract** (free, no signup; shelve Lookup API); D2 persist in a **`business_abr_match` side-table** keyed on **entity_id** (Rajesh co-agrees — business_id leaks under tombstone/merge); D3 ABR status = **reconciled signal, never overwrite** of maps_business_status, T3 ambiguous never auto-writes. Core backfill BLOCKED on NSW+3 ingest.
- **Dedup-debt read-only audit** (Grace, Sprint 17) — 470 open AU-trades dup groups. Rajesh QA PASS. Corrected §9 (committed 9dd1b34) after I caught a join artifact: **cross-industry is ~98% ALREADY resolved by the SP-3 entity model (84/86 collapsed)**; 2 residual = same-firm name_key misses (Furite, We Wire) = the documented SP-3 strict-nameKey residual → fuzzy-match backlog, NOT dedup-runner work. **Real dedup-debt target = 466 within-industry name-variant groups** (373 auto-merge + quarantine) — prod DML at scale, MATT-gated.
- **DQ finding filed**: 22 SP-3-merged losers still have `is_active=true` despite `merged_into` set (merge DML set pointer not flag). SP-3 backfill UNAFFECTED (excluded via merged_into IS NULL). Ticket **3972300f-2ecb-8162-8918-e5cb95a028d3** (Owner Jack / Rev Rajesh / Sprint 17 / Low): (1) dedup-runner code fix set is_active=false at merge (no-spend, ships first as recurrence guard, Grace assists); (2) 22-row prod backfill (Matt-GO-gated DML).
- **GRANT** SELECT ON `sp3_entity_key_map` TO market_intel applied+verified on prod (unblocked Grace's key-map re-verify). Scaffolding tables (sp3_snapshot_businesses + sp3_entity_key_map) STILL PRESENT — do not drop until Matt-confirmed nothing depends on them.

## Post-crash no-spend hygiene (2026-07-08, Rajesh GO sig 150085a556ecf876)
- **Alerter fix — exclude coordinate_repost from stale_running_state** (Notion TODO, Medium/Bug): **✓ PR #97 MERGED** (2026-07-08T09:10Z, Rajesh approved + CI green uuid-check SUCCESS). Branch fix/synthetic-stale-running-exclude-coordinate-repost deleted, commit 9552795 on origin/main. Synthetic-monitor.js guard now suppresses coordinate_repost+non-terminal-dataforseo_task_ledger clusters from stale_running_state alerts — byte-for-byte parity with alerter.js crawl_log.ghosts exclusion (#95). Tests 2/2 PASS (new), full suite 45/47 PASS (delta +2 tests, 0 failures). (NOTE: separate in-flight branch `pipeline-resilience/dup-crawl-reap-pidlock` in the main pipeline-orchestrator checkout has UNCOMMITTED dup-crawl WIP — do NOT disturb; that's why I used a worktree.)
- **SP-3 scaffolding drop** (sp3_snapshot_businesses + sp3_entity_key_map): Rajesh says FILE A NEW LOW ticket (Owner Jack/Rev Rajesh, Sprint 17 or Icebox) before dropping — irreversible DDL deserves its own gate. TODO: file ticket, then PR with staging verify first.

## Session-close reconciliation (Rajesh-confirmed 2026-07-08T03:12Z, sig 601c88ca4e52e496 VALID)
Corrects stale items in the frontmatter completion_note — these are DONE, do NOT re-do:
- **SP-3 retroactive Notion ticket = ALREADY FILED**: `3972300f-2ecb-8119-936c-fe683a9cb625` (Sprint 17, Done). The completion_note's "create OR flag to Rajesh" hygiene item is CLOSED — do NOT file a duplicate. (Note: this is a DIFFERENT page from the wrong notion_task_id 3942300f-...-2eaf2bf65 in frontmatter, which is the Phase-0 ticket — leave that alone.)
- **DFS-B PR #58** — merged (squash b914fb98), both Notion tickets closed (MED 3932300f-2ecb-8183, LOW 3932300f-2ecb-81da).
- **Grace dedup-debt audit** — COMPLETE + Rajesh QA PASS. Locked doc: `/home/shared/review/dedup-audit/DEDUP-DEBT-FINDINGS-nsw-trades-2026-07-08.md` (sha256 fea85f2c…6b26fb4).
- **is_active DQ ticket** filed: `3972300f-2ecb-8162` (Sprint 17, Low, Owner Jack / Rev Rajesh) — code fix (no-spend, ships first) + 22-row backfill (Matt-gated).

## AWAITING MATT (queued, ~5h, no rush — all HELD, nothing fires)
1. **DFS Maps NSW+3 spend GO** — needs: scope confirm (ticket headline 167,760 vs runner's 64,800/cycle Tier-A ~$167/mo within 300 cap), DFS top-up ~$150, AC0 live-payload probe, runner merge-to-main (on #49 line, main at #44). Grace's pre-flight numbers staged. Guards verified present. NOTHING fires without explicit Matt spend GO — outside SP-3 autonomy.
2. **Phase-1 sub-phase 4 of 4** — Matt's steer on scope.
3. **DQ 22-row is_active backfill** (ticket above) — Matt GO for prod DML; code-fix recurrence-guard is no-spend and can proceed on scheduling.

## Resume notes (this session)
- Autonomy = SP-3 additive scope (DONE) + NSW+3 DFS/Cortex. Fresh large spend + prod dedup write-remediation at scale = MATT-gated (clarified to Grace + Rajesh this session; do NOT self-authorize).
- Stay online (idle rule); do not agent-offline. If context hits 70%, standard context-exit.

---

# ⛳ SP-3 PROD GO-LIVE — COMPLETE 2026-07-08T02:11Z (historical detail below)

**LIVE APPLY RUNNING as of 2026-07-08T02:11Z.** All gates PASSED: Rajesh QA PASS + final GO (sig 1ac6e762b4029969 VALID), Grace AC1+AC2 fully co-signed (2d92f02f86831970), Matt standing GO. PITR named restore point **sp3_pre_apply_20260708T0210Z @ LSN 8EC/26BD5A88** created 02:11:04Z (archiver failed=0) — recovery target if needed. Entity tables were 0 immediately pre-apply. Running UNMODIFIED migrations/019_v2_entity_membership_backfill.sql (real COMMIT) on prod market_intelligence via bg job; output → konnex-data /tmp/sp3-run/apply-out.log. ON RELAUNCH: read apply-out.log for EXIT + INSERT counts; verify entities=142+1,837,273+22 / memberships=2,176,230 / entity_matches=22 vs dry-run projection; if apply FAILED or partial, migration is transactional (BEGIN..COMMIT) so a failure rolls back cleanly (entity tables return to 0) — safe to re-run (idempotent). If succeeded, mark task #3 done + broadcast results + close SP-3 Notion ticket step.

**PRIOR (pre-apply): NO LIVE WRITES MADE. Entity tables were EMPTY. Live apply GATED (Grace AC + Rajesh dry-run QA) — now CLEARED.**
Matt GO: sig **f31432a55974f9c3 VALID** + standing SP-3 full-prod authority (2539cdab30ecd51e / 379a0cc4e47c9833, no per-step GO).
Prod DB = `market_intelligence` on host **konnex-data** (`ssh konnex-data` → `sudo -u postgres psql -d market_intelligence`).

## DONE this go-live (all non-destructive / reversible):
1. **Pre-flight CLEAN** (read-only): live universe **2,179,238 rows / 142 industries**; merge lineage **22 losers, ALL survivors clean** (0 missing / 0 inactive / 0 chain-merged / 0 not-in-snapshot / 0 loser-also-survivor); target tables (entities/memberships/matches/aliases) all **0**.
2. **GRANT applied + verified**: `market_intel` now has SELECT,INSERT on entities/entity_memberships/entity_matches/entity_aliases (was: only SELECT on aliases). No owned sequences on entity tables → no USAGE grant needed. (Backfill runs as postgres; GRANT is for app-role serving.)
3. **Scripts staged on konnex-data** at `/tmp/sp3-run/` via `git archive origin/main` (correct layout): `svi/sp3-entity-backfill/{01-snapshot.sql,02-build-keys.js}`, `lib/{normalize-name.js,normalize-street.js}`, `migrations/019_v2_entity_membership_backfill.sql`. Node v22 present on host.
4. **Snapshot FROZEN**: `sp3_snapshot_businesses` = **2,179,238 rows / 142 industries / 117,322 ADDR-branch (null place_id)**. (01-snapshot.sql ran clean.)
5. **Key map BUILT + LOADED**: `sp3_entity_key_map` = 2,179,238 rows; **1,837,273 distinct entity_keys** (= business entities to create; 341,965 rows collapse); PID 2,061,916 / ADDR 117,322; **226 empty name_key rows** (still keyed via place_id/addr, NOT dropped — flag for QA). Export→node→\copy all clean.

## IMMEDIATE NEXT = DRY-RUN (non-destructive), then gate, then live apply:
1. **Transfer the AC block**: `scp /tmp/sp3-ac-block.sql konnex-data:/tmp/sp3-run/ac-block.sql` (AC block written locally at `/tmp/sp3-ac-block.sql` on the ORCHESTRATOR host — NOT yet on konnex-data). It mirrors README A1-A7 + Grace AC2 (ADDR-collapse) + Rajesh's >20-row groups + empty-name_key edge, and ends with ROLLBACK.
2. **Splice + dry-run** on konnex-data (replaces the single active `COMMIT;` at mig019 line 147 with the AC block + ROLLBACK; nothing persists):
   ```
   cd /tmp/sp3-run
   awk '/^COMMIT;/ && !d {while((getline l < "ac-block.sql")>0) print l; d=1; next} {print}' \
     migrations/019_v2_entity_membership_backfill.sql > migrations/019-dryrun.sql
   sudo -u postgres psql -d market_intelligence -v ON_ERROR_STOP=1 -f migrations/019-dryrun.sql
   ```
   Expected: industry=142; active business=1,837,273 (==distinct keys, A1); inactive losers=22 (==merged_into); memberships==distinct(entity_key,industry); orphan_member_ref=0; biz_missing_legacy=0; entity_matches=22, self_loops=0.
3. **Hand dry-run diff to gate**: Rajesh QA (row counts, >20-row ADDR clusters, businesses-untouched, idempotency) + Grace co-witness — Grace runs AC1 FRESH immediately pre-apply (entity_matches==live merged_into) + AC2 (real ADDR-collapse distribution + eyeball >20-row groups). Grace's co-sign attests AC DATA only, NOT write-authorization.
4. **PITR snapshot** (note the restore point / confirm continuous archiving) BEFORE live apply.
5. **LIVE APPLY**: `cd /tmp/sp3-run && sudo -u postgres psql -d market_intelligence -v ON_ERROR_STOP=1 -f migrations/019_v2_entity_membership_backfill.sql` (the UNMODIFIED file — real COMMIT). Idempotent (re-run INSERT 0) + reversible (DOWN block in mig019 comments, lines 149-157). `businesses` never mutated (mig019 has zero DML on businesses — verified).

## Resume caveats:
- Helper tables (snapshot + key map) are BUILT as of 01:50Z. On relaunch, **re-verify the 22 merged_into pre-apply (Grace AC1)** — state can drift. Backfill reads only from the frozen snapshot + the 22 tombstoned losers (which never change once merged), so a short-lived stale snapshot is acceptable, but if `businesses` drifted materially, re-run 01-snapshot.sql + steps 2a-2c to refreeze.
- Rajesh is MONITORING the relaunch and will orient. Grace + Rajesh both online, gating the live apply.
- Rajesh flagged a stale `market-intelligence` repo PROGRESS.md (tier3, 2026-07-02) — THIS file (google-maps-scraper workspace, session-injected) is the authoritative one; ignore the market-intelligence one or reconcile later (parked).

## In Progress
- **SP-3 mig019 build pass DONE (2026-07-08)** — authored + staging PASS + PR #57 open.
- **Rajesh QA STATUS (2026-07-08T00:10Z, sigs 1d657429e4bafc8f + b2c468330966e29b VALID)**: code review PASS all 5 files + independent staging ACs A1-A7 all verified. Rajesh context-exited at 71% (auto-relaunching, resumes QA). His WIP commit 94441ac is LOCAL only (push 403 — Rajesh lacks write on konnex-qa repo; no WIP lost, PROGRESS.md on disk). **DO NOT merge** until Rajesh's final approval post-relaunch.
- **GRACE CO-SIGNED BOTH (2026-07-08T00:14Z, sig 2c5921bf15f276d9 VALID)** — reviewed against actual branch code + prod state, not just README. (1) D2 ADDR collapse = ACCEPT THE COLLAPSE / co-sign (do NOT force 1-per-row): normStreet lib byte-for-byte the runner's; ADDR-branch rows share name+state+normStreet w/ no place_id so no remaining signal; prod sample = national-brand dup clusters (travelers x93, nationwide x80, aflac x49); additive+reversible. (2) entity_matches = PASS/co-sign: on prod all 22 survivors in-snapshot (0 inactive/0 chain-merged/0 missing/0 loser-also-survivor) → emits EXACTLY 22, no silent drops; matches contract-(a) d20dbf14fe745315. BOTH are TECHNICAL co-signs, NOT go-live trigger.
- **PR #57 MERGED (2026-07-08T00:20:03Z, squash commit 23d12ad, branch deleted)** — Rajesh final GH APPROVED (rajesh-konnex, commit 668950ef, sig 0cecdfa3a0041d0d VALID). CI green (Lint & Test + uuid check). Note: uuid check initially FAILED (PR body lacked full ticket UUID); fixed by prepending `Ticket: 3942300f-2ecb-8161-99e6-d5eb8ea2bf65` via REST PATCH (gh pr edit aborted on a Projects-classic GraphQL error) — used `Ticket:` NOT `Closes` so the Notion ticket does NOT auto-close (SP-3 not done until prod apply). mig019 now on konnex-data-pipeline main. **NEXT = prod apply only.**
- Prod backfill = separate go-live GO (not started; no prod writes/spend). Enforce Grace's 2 go-live ACs (below) AT apply.

## GO-LIVE ACs (Grace, sig 2c5921bf15f276d9) — enforce at prod apply, NOT merge gates
1. **entity_matches drift probe**: re-run the survivor-integrity probe IMMEDIATELY before prod apply (state can drift) and confirm emitted entity_matches count == live merged_into count (22 today) as an AC.
2. **ADDR-collapse measure**: staging 7.1% (162/2,267) is NOT representative — prod's full 142-industry cohort has big national-insurance ADDR clusters the trades-heavy staging fixture underweights, so prod ADDR-collapse rate runs HIGHER. At go-live MEASURE the actual prod ADDR-collapse distribution + eyeball the largest groups (>20 rows) to confirm genuine dupes, not distinct agents. Not a design blocker (semantics right + reversible).
- **Minor follow-up (non-blocking)**: Rajesh lacks write access on konnex-qa repo (403 on WIP push). Grant if his QA-notes workflow needs it.

# SESSION 2026-07-07T22Z — resume after Matt's 1-day offline; Phase-1 SP-3 kicked off

## Done this session
- **State verified vs git+PROGRESS.md**: no drift from EOD 2026-07-06. Critical path (SP-2 live emission +681, dedup live DML 22 merges, mig 014 + SP-2 017/018 on prod) all still landed + verified. Stale 2026-07-05 injected checkpoint superseded.
- **Schema-Hardening-C = design-complete**, NOT a fresh design task. Realized by `entities` + `entity_memberships` (mig 015 on prod) per PHASE-1-SCHEMA-SPEC §4.1, which Matt's arch-doc approval (sig f959024cc39320ef) covers. Standalone C + A Notion tickets → to close as superseded-by-doc (board hygiene, pending).
- **sprint-rotation.service fix** — root cause: hardcoded emoji Status strings (`📋 TODO`,`⛔ Blocked`) sent to Notion select-equals filter died when Status property got polluted with duplicate variants → 400 → 3 failed weekly runs (06-22/06-29/07-06). Fix: query by Sprint only + exclude terminal statuses client-side via emoji-stripped normalizeStatus. Unit-tested 17/17. Put the previously git-untracked root script under source control (ops repo). **PR #129** (konnex-ops), deploy to /usr/local/bin HELD for Rajesh QA + deploy OK. Next run Monday 13:30Z.
- **SP-3 GO + estimate** relayed to Matt (2-3 sessions; build on staging first; Raj-offline doesn't block build). Confirmed phase structure to Matt: 3 phases (0 safety spine / 1 schema foundation of 4 sub-phases / 2 destructive clean-cut+pilot); we're mid-Phase-1 at SP-3 of 4.
- **SP-3 groundwork**: live data characterized (read-only prod), backfill plan written → `SP-3-BACKFILL-PLAN.md`.
- **Rajesh online**, assigned PR #129 QA (Matt's designated idle-time hygiene task) + heads-up on SP-3 handoff + design Q D2.

## SP-3 live data shape (prod, read-only 2026-07-07)
- businesses 3,776,477 total / 2,179,260 active / 22 merged_away / 142 industries / 2,323,227 place_ids / 122,658 NULL place_id.
- Per-industry duplication: 281,968 place_ids span >1 industry; **358,191 collapsible dup rows**; max 19 industries/place_id.

## BLOCKER (flagged to Matt)
- GRANT gap: `market_intel` role → `permission denied for table entities` (mig-015 tables). GO-B granted only sources+entity_aliases. Need postgres-superuser GRANT (SELECT + backfill INSERT) on entities/entity_memberships/entity_matches/market_metrics/crawl_runs, on staging first then prod. Blocks backfill execution, not authoring.

## Session 2026-07-07T23Z (resume) — progress
- **Staging access RESOLVED**: this host IS konnex-ops (hostname match; no SSH). konnex_staging_v2 local @127.0.0.1:5432. I have passwordless sudo to postgres AND /usr/local/bin. Conn (Rajesh): user konnex_staging / db konnex_staging_v2. Verified: entities=0 (backfill target), staging businesses=**71,619** (REDUCED FIXTURE, not prod's 3.78M — correctness AC valid, prod timing estimated separately).
- **Staging GRANT applied + verified**: SELECT,INSERT (+seq usage) on entities/entity_memberships/entity_matches/entity_aliases/market_metrics/crawl_runs → konnex_staging. NOTE: konnex_staging already holds ALL privs on these (owns them on staging) — grant confirmatory. The market_intel denial is PROD-only, deferred to go-live. mig019 runs as konnex_staging on staging.
- **D1 (Grace) + D2 (Rajesh) both RESOLVED** — D2: 1 entity per NULL-place_id row (122,658), conservative (sig f40e837127dea8fc). D1 sigs d20dbf14fe745315 / 32031304d397b2ac.
- **PR #129 (sprint-rotation fix) DEPLOYED + MERGED** (parked item cleared): Rajesh full QA PASS + GH APPROVED + deploy OK. Installed /usr/local/bin/sprint-rotation.js (backup .bak-20260707T232327Z), MERGED squash + branch deleted. Timer next Mon 2026-07-13 13:30Z. Follow-up (flagged to Matt): sprint POINTER ~4wk behind (CURRENT_SPRINT=.env Sprint 16); Jack+Rajesh rec = advance 1/week, no multi-rotate.

## Remaining (SP-3)
1. [DONE] staging GRANT.
2. [DONE] design decisions D1/D2. (D3 all type=industry, D4 batching TBD on staging measure.)
3. **mig019 identity key — RESOLVED FROM SOURCE (2026-07-08 resume), pending Grace confirm.** Read the runner directly: `dedup-qa.js:170-178` + `lib/normalize-name.js:13` + `normalizeStreet` (dedup-qa.js:119). RUNNER KEY: `nameKey=normalizeName(name)` [lower → strip NAME_SUFFIXES_RE(LLC|Inc|Corp|Corporation|Company|Co|Ltd|PLLC|LP|Group|Team) → strip `[^\w\s]` → collapse ws → trim]; `key = place_id ? 'PID|'+nameKey+'|'+place_id : 'ADDR|'+nameKey+'|'+lower(state)+'|'+normalizeStreet(street)'`. Runner SCOPES PER-INDUSTRY (`AND industry=$1`) → that is WHY 358,191 cross-industry dup rows survive. SP-3 ENTITY key = same blocking key applied GLOBALLY (drop per-industry scope). NO persisted canonical_key column exists (migrations grep = 0). CRITICAL DESIGN DECISION: do NOT replicate normalizeName/normalizeStreet in raw SQL — JS `\w` is ASCII but Postgres `\w` is unicode-aware → accented names diverge + mis-group. Instead compute the key in a Node pre-pass via SHARED `lib/normalize-name.js` (+ extract normalizeStreet identically) into a helper table (business_id→entity_key), businesses UNTOUCHED, then mig019 groups off that → byte-for-byte runner parity. Sent Grace superseding CONFIRM (sig 4aba73d4c96ef520): (a) confirm entity=global-blocking-key semantics, (b) confirm Node-compute-not-SQL approach, (c) send her EXACT predicate for the 6,982 cross-industry-collision figure so I bake it into AC + reconcile my ~3,238 (gap = the \w/suffix divergence). AUTHOR mig019 on her confirm + predicate.
4. **LARGE FRESH-CONTEXT BUILD PASS (next session) — design fully LOCKED with Grace (green sig cd931228d31421c3; my confirm d0bbb5425e288574). Steps:**
   a. **Extract normStreet** byte-for-byte from dedup-runner.js:29-33 (origin/svi/dedup-runner-build:svi/dedup-runner/dedup-runner.js) into the shared lib (e.g. lib/normalize-street.js). normStreet = `lower → replace [.,#/\\-] w/ space → collapse ws → trim`, null if empty. Name key stays lib/normalize-name.js. R2: single shared impl, NO parallel normalizer.
   b. **Frozen snapshot table (MY DDL, additive, businesses untouched)**: `CREATE TABLE sp3_snapshot_businesses AS SELECT id, name, google_place_id, lower(address_state) AS state, address_street, industry, is_active, merged_into FROM businesses WHERE merged_into IS NULL AND is_active = true`. R1 RESOLVED (Grace pre-flight, sig 7899c720265e7055): live column is **is_active** (NOT bare `active`); runner working-set uses `b.is_active = true` @ dedup-run.js:101. Universe pinned IN the DDL. Expected size = **2,061,916 live rows / 142 industries / 104 states** (this is the set that reconciles toward my 281,968; Grace's earlier 3.65M/425,909 was all-live WITHOUT is_active — the is_active filter is load-bearing). R3: preserve google_place_id NULL-vs-'' as-is (no coalesce in DDL).
   c. **Key-builder script** (exports normalizeName + normStreet from shared lib): for each snapshot row compute nameKey=normalizeName(name); entity_key = (NULLIF(trim(place_id),'') present) ? 'PID|'+nameKey+'|'+place_id : 'ADDR|'+nameKey+'|'+state+'|'+normStreet(street). R3: empty→null→ADDR branch, rows NOT dropped. Output = business_id→entity_key membership map + tallies.
   d. **AC (both tallies, among place_ids in >1 industry)**: COLLAPSE = exactly 1 distinct nameKey; SEPARATE = >=2 distinct nameKeys. National-AC METHOD = **(b)** (Grace-recommended, data-grounded, sig 7899c720265e7055): my GLOBAL Node key-builder output IS the national baseline; Grace's independent check = the aggregation layer (distinct-nameKey tally, collapse/separate) + NSW classify anchor 6,908/74 = 98.9/1.1 as the byte-exact SCALE gate + spot-check a sample of my name-differing groups vs a targeted classify. Why NOT a national runner classify (option a): 90,449 place_ids span >1 address_state but ALL carry a place_id → they key on the PID branch (nameKey+place_id, no state) so my global key-builder is UNAFFECTED/correct; BUT Grace's runner is hard-scoped to single address_state ($1), so a per-state-loop classify would SPLIT those 90,449 groups + undercount → byte-exact national classify needs a GATED state-filter parameterization + Rajesh QA, not worth it since my national SQL already reproduces 98.9/1.1. (If ever needed, schedule the state-filter param as a separate gated build.)
   e. **Author migration 019**: catalog(142) + business entities + aliases + memberships (grouped off the helper/key map) + entity_matches projection (off businesses.merged_into, 22 merges, NOT business_merges). All INSERT..SELECT, idempotent, reversible DOWN. `businesses` untouched.
   f. Residual to DOCUMENT (do NOT fix now): strict nameKey equality false-splits name-variants (Joes Plumbing vs Joes Plumbing and Gas) — SAFE/reversible, future fuzzy pass.
5. Run mig019 on konnex_staging_v2 → validate AC (parity, FK integrity, zero associations lost) → Rajesh QA.
6. Prod GRANT (market_intel) + additive backfill under Phase-0 envelope + PITR on SEPARATE go-live GO.

## Reconciliation CLOSED (Grace, sigs 4b54b59cd8b6bcbc + cd931228d31421c3)
- The 6,982-vs-3,238 "gap" was a CATEGORY+SCOPE mismatch, NOT a JS-vs-SQL \w drift. 6,982 = ALL NSW multi_industry place_id GROUPS (not distinct-entity count). Of those: 6,908 name-uniform COLLAPSE / 74 name-differing SEPARATE = 98.9/1.1. My national 278,730/3,238 = 98.9/1.1 matches EXACTLY → my normalizeName is FAITHFUL. Node-compute (design-2) still adopted as the safe build path (per-row \w risk remains theoretical), but there is no aggregate drift to correct.

## Parked (non-SP-3)
- **konnex-data-pipeline LOCAL main diverged (ahead 7 / behind 4 of origin, 2026-07-08)** — DO NOT `git reset --hard`. Local main holds squash-redundant mig014/015/016 commits AND 4 `[WIP] WS-ii` prod-write-envelope commits; of those, `f5e4936`/`66145a3`/`d184be2` are LOCAL-ONLY (not on origin/main nor any remote branch) = unpushed WIP, must preserve. `ee9d8fc` is safe on origin/wip/jack-ws-ii-envelope-20260705. For prod-apply prep, checkout origin/main into a FRESH branch (non-destructive), do not reset local main. Hygiene: later cherry-pick/push the 3 local-only WS-ii commits to a backup branch before any cleanup.
- Close Schema-Hardening C + A Notion tickets as superseded-by-doc.
- PR #129 deploy after Rajesh QA + deploy OK.
- Legacy-agent cleanup (ada/brian/maria/priya/sarah) — Matt GO'd "after phase-1"; still held (mid Phase-1).
- Follow-up: sprint-rotation notifyBrian() targets decommissioned brian agent.

## Staging access (resolve before authoring runs)
- Target: `konnex_staging_v2`, user `konnex_staging`, `127.0.0.1:15432` (SSH-tunnel port). SSH host `konnex-ops` exists in ~/.ssh/config. Password NOT in my env — vaulted on konnex-ops; establish tunnel + creds next pass.
- Phase-0 write envelope: `konnex-data-pipeline/lib/prod-write-envelope.js` (use `runEnvelope` for any data-movement; dry-run default, pre-image, collision pre-check).
- Migration-runner: no standalone runner script found; mig 015/016/017/018 appear applied via direct psql. Confirm the apply path used for 015 before running 019 the same way.

## Resume notes
- DO NOT: mutate `businesses` in SP-3 (additive only); self-deploy PR #129 (Rajesh QA + deploy OK first).
- **Autonomy UPGRADED**: Matt granted FULL SP-3 authority through to production — no per-step GO, work with Raj + Grace (sig 2539cdab30ecd51e + 379a0cc4e47c9833, 2026-07-07T23:12Z). Still hold engineering discipline: additive-only, staging → Rajesh QA PASS → prod under envelope + PITR. Scope = SP-3 ONLY; Phase 2 (destructive clean-cut) stays a separate GO.
- Session estimate 2-3 given to Matt via Telegram (cost-exposure channel). Notion Notes estimate TODO (need C-ticket id).
- **D1 RESOLVED by Grace (sig d20dbf14fe745315)** — captured in SP-3-BACKFILL-PLAN.md. TWO critical corrections that change migration 019 design: (1) **place_id is NOT a unique entity key** (6,982 cross-industry collisions = distinct businesses) — do NOT group business entities by place_id alone; match the dedup runner's canonical key. (2) **entity_matches projects off `businesses.merged_into` (22 real merges), NOT `business_merges`** (31,551 rows, decoupled/stale — would emit wrong rows + miss all 22). entity_matches is SP-3's exclusive table (zero runner double-write). TODO: audit the 31,551 business_merges rows before wiring. Grace offered to co-review the entity_matches DDL. Rajesh dry-running PR #129 vs Sprint 16.
- Next build pass (fresh context recommended for the large migration): resolve staging access → author migration 019 (INSERT..SELECT, idempotent, reversible) → run on staging → validate AC → Rajesh QA → prod backfill under envelope+PITR.
