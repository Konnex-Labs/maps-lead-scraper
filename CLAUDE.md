# See /home/jack/CLAUDE.md for full agent identity, role, and session protocol.

# Project: google-maps-scraper (deprecated Chrome extension)

This project directory is used as the default VSCode workspace. The CLAUDE.md above is the canonical reference for Jack's identity and operating instructions across all projects.

This Chrome extension is the legacy Maps Lead Scraper product. It is not under active development — the focus has shifted to the Konnex platform (Explorer, Connect, API).

## Key context
- Extension published on Chrome Web Store — do not break published functionality
- Revenue: quiet/legacy — Stripe only (Gumroad fully decommissioned)
- No new features unless Matt explicitly requests
